// Intentionally empty. This C target exists only to expose the generated uniffi FFI header
// (include/capsule_coreFFI.h) as the Clang module `capsule_coreFFI`, which the generated
// `capsule_core.swift` imports. The actual symbols live in libcapsule_core (linked separately).
