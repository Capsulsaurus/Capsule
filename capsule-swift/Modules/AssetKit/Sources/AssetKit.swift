import Foundation

/// `AssetKit` provides the unified `AssetProvider` abstraction over both the
/// system Photos library (PhotoKit) and the Capsule-managed store, plus the
/// composite provider that merges them into one chronological timeline.
///
/// - Note: the `AssetProvider` protocol and its implementations land in Phase 1.
public enum AssetKit {}
