import Foundation

/// `ImagePipeline` decodes, downsamples, caches, prefetches, and cancels
/// thumbnail and full-resolution image requests — the performance core of the
/// photo grid and viewer.
///
/// - Note: implemented in Phase 2.
public enum ImagePipeline {}
