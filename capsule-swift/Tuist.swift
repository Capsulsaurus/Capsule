import ProjectDescription

/// Tuist configuration for the Capsule Apple client.
///
/// Its presence also marks `capsule-swift/` as the Tuist project root, so the
/// generator does not walk up into the surrounding monorepo.
let tuist = Tuist(project: .tuist())
